{
  "name": "project_player_desktop",
  "private": true,
  "version": "1.0.0",
  "description": "A multi-modal streaming player for local resources",
  "author": "Isshin Player Team",
  "scripts": {
    "dev": "tauri dev",
    "build": "yarn --cwd frontend build",
    "tauri": "tauri",
    "dist": "tauri build",
    "dist:mac": "tauri build --bundles dmg,app",
    "dist:win": "tauri build --bundles nsis,msi"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
